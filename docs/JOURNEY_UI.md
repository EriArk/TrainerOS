# Journey and achievement presentation

P1 #47/#48/#64 adds a Journey entry to Hall while preserving the manual archive and live RetroAchievements account/cache service. This is not a new save parser, Champion detector or ownership migration.

## Data boundaries

Journey consumes the same current-Adventure presentation as Home. Existing verified FireRed/Emerald badge bits, exact badge-set artwork and Caught totals can appear here immediately. No new title coverage is claimed. Missing, checking, unreadable, unsupported or differently selected provider data cannot become known progress; zero remains distinct from unknown. Badge assets remain separate from game screenshots and sprite animations.

Save playtime and milestones remain explicitly unknown. Process runtime is not save playtime. No completion percentage, badge acquisition date or Champion victory is inferred from the badge count. Current-save history, rollback lineage and additional semantic fields remain P8.

Champion records have a production empty state and an isolated development sample/list/detail route. The sample carries an explicitly fictional historical team, unknown victory date and separately labelled observation date. It is not derived from today's Party or a manual Hall memory. Production cannot enable it through restored navigation. Durable exact-build/playthrough Champion storage and a real provider remain P8; no record is created by opening this UI.

RA continues to browse the existing account collection across confirmed matched Adventures. The UI states this scope; shared Y does not pretend to supply the later exact selected-Adventure projection. Earned/locked/unknown colors and detail symbols accompany textual Standard/Hardcore/unknown evidence. Existing same-account offline/cache and wrong-account/set rejection remain authoritative. There is no new earning, refresh protocol, account write or notification behavior.

## Controller routes

- New Hall navigation opens Journey. Existing persisted manual archive or RA routes are preserved.
- Journey A opens Adventure memories; X opens Champion records; Select creates a manual memory. Directional presses cannot divert the fixed A action.
- Champion sample A opens historical detail. B returns detail → records → Journey. Production empty A/B returns to Journey. No destructive action exists here.
- Manual archive keeps list/detail/Select new/X edit behavior. B returns detail → list → Journey, retaining the chosen memory.
- L2/R2 switches the whole Journey/Hall face with RA and preserves the local route. L1/R1 remains global. Shared Y, editor, keyboard, account and system-menu priority remain unchanged.
- Navigation persists route/focus only, without a database schema change or new owned snapshots.

## Acceptance

Exercise Journey and Champion routes through SDL, page/face return and restart, manual archive writes/cancellation, old navigation compatibility, fixed actions and absent data. Inspect large badge art and known-zero/unavailable states without inventing a universal badge count. RA must retain earned and unknown states through offline/failed refresh and reject another account's data. Device captures and injected input do not substitute for the owner's physical-button acceptance.

2026-09-20 checks: Windows 35/35; ARM64 38 entries plus the separate persistence scenario. The final spacing adjustment additionally passed both affected ARM rendered/persistence scenarios. The native Flip Gamescope Hall scenario passed without QML warnings. Rendered captures cover sample Champion detail, independent Hall/RA routes, account/error/offline cases, and 960×540 Journey badges with earned/zero/unavailable observations. Native 1920×1080 captures were visually inspected.

Production was installed with a verified binary/database backup. InputPlumber events exercised the preserved real RA detail, Journey with the existing FireRed 8-badge/386-Caught observation, the empty Champion view and manual-archive Back route. No sample Champion appeared in the personal library. Installed binary hash, database integrity, Trainer profile, manual archive and all 686 Adventure registrations were checked; the device was returned to Home. Physical human-button review and additional provider capabilities remain open.
