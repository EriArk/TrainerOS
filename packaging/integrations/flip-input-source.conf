# The physical Flip source is grabbed by InputPlumber. SDL applications must
# use its virtual controller even when the exit helper has read-only access.
SDL_GAMECONTROLLER_IGNORE_DEVICES=0x2020/0x3001
